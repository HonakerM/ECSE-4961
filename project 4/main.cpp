#include "main.h"

#include "worker.h"

int main(int argc, char ** argv){

    //create worker
    DictionaryWorker worker = DictionaryWorker(1);
    //worker.encode_file("input", "output");
    unsigned int i = -1;
    std::cout << i <<std::endl;
}